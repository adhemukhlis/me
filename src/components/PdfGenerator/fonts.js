import { bold, normal } from '@/assets/fonts/NotoGeorgian'
export const Roboto = {
	normal: Buffer.from(normal, 'base64'),
	bold: Buffer.from(bold, 'base64'),
	italics: Buffer.from(normal, 'base64'),
	bolditalics: Buffer.from(bold, 'base64')
}
